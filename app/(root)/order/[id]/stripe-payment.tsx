const StripePayment = ({priceInCents, orderId, clientSecret}:{
    priceInCents: number,
    orderId: string,
    clientSecret: string
}) => {
    return ( 
        <>
        stripe form
        </>
     );
}
 
export default StripePayment;